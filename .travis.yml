language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk6
install: true
sudo: required
before_install:
  - export JAVA_HOME=/usr/lib/jvm/java-8-oracle
  - mvn clean install
cache:
  directories:
  - $HOME/.m2

script:
  - bash <(curl -s https://codecov.io/bash)
