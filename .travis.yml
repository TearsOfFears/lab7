language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk6
install: true
sudo: required
before_install:
  - mvn clean install
cache:
  directories:
  - $HOME/.m2

script:
  - mvn clean install
  - bash <(curl -s https://codecov.io/bash)
